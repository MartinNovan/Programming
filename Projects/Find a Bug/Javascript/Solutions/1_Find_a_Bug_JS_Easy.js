function addNumbers(a, b) { //Function is supposed to add two numbers, but in original they were substracted
    return a - b; // Only thing you need to do is to rewrite '-' to '+' and it's done
}
console.log(addNumbers(5, 3)); // Expected result: 8